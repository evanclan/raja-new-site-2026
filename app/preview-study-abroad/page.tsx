// TEMPORARY verification route — renders only the Kaeru Ryugaku (Study
// Abroad) panel so the section can be reviewed in isolation while other
// sections are being edited. Safe to delete; not linked from anywhere.
import { StudyAbroad } from "@/components/sections/StudyAbroad";

export default function PreviewStudyAbroadPage() {
  return <StudyAbroad />;
}
