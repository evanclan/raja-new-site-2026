// TEMPORARY verification route — renders only the Let's Go English panel so
// the section can be reviewed in isolation while other sections are being
// edited. Safe to delete; not linked from anywhere.
import { English } from "@/components/sections/English";

export default function PreviewEnglishPage() {
  return <English />;
}
