// TEMPORARY verification route — renders only the Clab panel so the
// Carat spotlight can be reviewed in isolation. Safe to delete.
import { Clab } from "@/components/sections/Clab";

export default function PreviewClabPage() {
  return <Clab />;
}
